# Work log

> Last verified: {{TODAY}}

This file is **{{NAME}}'s** running log in **this repo**. Agents append
here. Do not edit `sgs-agent-skills` on GitHub to record work.

`admin/plans/` is how to implement a build. This log is what actually
happened (sessions, extracts, fixes, leftover IT items).

## How to append

After every session that changed something, add a dated section
(timezone explicit):

```
## {{TODAY}} (timezone)

- What we did
- What we changed (paths)
- Left for later
```

Newest at the top.

## {{TODAY}}

- Agent workspace set up from sgs-agent-skills. CONTEXT/ and this log
  are local to this repo.
