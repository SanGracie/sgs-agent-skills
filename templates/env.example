# Local secrets for this repo. Copy to `.env` (gitignored).
# Never commit `.env`. Setup creates `.env` from this file with blanks
# if you do not already have one. Fill values on the machine — do not
# paste passwords into Cursor chat.

# Accutest AniTa (SEE SALES / TAT-by-group skills)
ANITA_USER=
ANITA_PASSWORD=
ANITA_HOST=10.149.0.5

# Outlook datadrop forward (optional override)
# DATADROP_TO=us.ehs.datadrop@sgs.com
