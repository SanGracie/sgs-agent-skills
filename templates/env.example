# Local secrets. Copy to `.env` (gitignored). Never commit `.env`.
# Setup creates `.env` from this file if you do not already have one.
# Fill passwords on the machine — do not paste them into Cursor chat.

# --- SGS identity ---
# Microsoft / Outlook / Azure DevOps / GitHub (SGS-linked) sign-in.
# Email and name only. Do NOT put your Microsoft / Entra password here;
# that stays in Windows / browser SSO.
SGS_EMAIL=you@sgs.com
SGS_NAME=Your Name
# SGS_UPN=you@sgs.com

# --- Accutest LIMS (AniTa) ---
# Host login for SEE SALES / TAT-by-group skills. This password DOES belong
# in `.env` (or User env). Never commit it.
ANITA_USER=
ANITA_PASSWORD=
ANITA_HOST=10.149.0.5

# --- Outlook datadrop ---
# DATADROP_TO=us.ehs.datadrop@sgs.com

# EHS Dashboard (Report Feedback for skill issues)
EHS_DASHBOARD_BASE_URL=https://us-ehs-tv-reports-hrc7gxa6dtesdfca.centralus-01.azurewebsites.net

# --- Workday expenses (reimbursements skill) ---
# Open in real Edge, not Cursor's helper window.
# EXPENSE_URL=https://wd3.myworkday.com/sgs/d/task/2997$728.htmld
# EXPENSE_USERNAME=you@sgs.com
# WORKDAY_EDGE_PROFILE=SgsWorkdayEdge

# --- Future skills (uncomment when something needs them) ---
# ADO_PAT=
# SP_CLIENT_ID=
# SP_CLIENT_SECRET=
# AZURE_TENANT_ID=
